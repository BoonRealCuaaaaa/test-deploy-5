export enum ChromeExternalMessageType {
  CONNECTION = 'connection',
  DISCONNECTION = 'disconnection',
}

export enum ChromeMessageType {
  COMMAND = 'command',
  GA4_EVENT_SENDING = 'ga4_event_sending',
  MESSAGE_QUEUE_SENDING = 'message_queue_sending',
}
