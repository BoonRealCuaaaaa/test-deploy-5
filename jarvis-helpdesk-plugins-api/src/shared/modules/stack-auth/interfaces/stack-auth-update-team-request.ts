export interface StackAuthUpdateTeamRequest {
  client_read_only_metadata?: Record<string, any>;
  server_metadata?: Record<string, any>;
  display_name?: string;
  profile_image_url?: string;
  client_metadata?: Record<string, any>;
}
